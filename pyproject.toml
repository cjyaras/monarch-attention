[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["sobalib"]

[project]
name = "soba"
version = "0.0.1"
dependencies = [
    "torch",
    "torchvision",
    "einops",
    "transformers",
    "sparsemax",
    "jupyter",
    "tqdm",
]
requires-python = ">=3.11"
authors = [
  {name = "Can Yaras", email = "cjyaras@gmail.com"},
]
description = "Stuctured optimal bi-directional attention."
readme = "README.md"
license = {file = "LICENSE"}
keywords = []


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ma"]

[project]
name = "monarch-attention"
version = "0.0.1"
dependencies = [
    "torch>=2.6.0",
    "einops",
]
requires-python = ">=3.11"
authors = [
  {name = "Can Yaras", email = "cjyaras@gmail.com"},
]
description = "Monarch attention."
readme = "README.md"
license = {file = "LICENSE"}
keywords = []

[project.optional-dependencies]
experiments = [
    "numpy",
    "tqdm",
    "matplotlib",
    "torchvision",
    "torch_geometric",
    "torchtnt",
    "transformers>=4.51,<4.57",
    "diffusers",
    "datasets",
    "evaluate",
    "nltk",
    "rouge_score",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
# Triton compiles a C launcher at runtime and needs Python headers, which
# system Pythons often lack; uv-managed Pythons always ship them.
python-preference = "only-managed"
